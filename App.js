import WelcomeScreen from './src/screens/WelcomeScreen';

// Punto de entrada de la app. A partir del Módulo 5 esto se
// reemplaza por el navigator; por ahora renderiza la pantalla
// de bienvenida del Checkpoint 1.
export default function App() {
  return <WelcomeScreen />;
}
